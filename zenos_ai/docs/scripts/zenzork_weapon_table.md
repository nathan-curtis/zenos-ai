# ZenZork Weapon Table — DUNGEONMIND's Armory (Such As It Is)

<!--
  OBFUSCATION NOTICE, READ BEFORE YOU DO ANYTHING RASH:

  Weapon entries tied to a real registered landmark (Carl's Left Sock
  is the only one that currently exists) are intentionally NOT part of
  this curtain — same precedent as the retro cartridge collectibles:
  landmark-tied content requires real household setup to ever surface
  in play, so browsing the flavor text in the repo doesn't actually
  spoil a discovery the way loot/swag/threat text does. This curtain
  exists for any FUTURE weapon entry that isn't landmark-gated — a
  general "improvised weapon" flavor addition, say. Recipe when that
  happens: md5(lowercase(trim("Item Name::Flavor Text"))), same family
  as the loot/swag/protect tables.
-->

Nothing curtained here yet. `unarmed` (the fallback for any carried
item not otherwise listed) has no flavor text of its own — it's the
default, not a discovery. Carl's Left Sock is documented in the main
readme and isn't hidden — see the precedent note above.

When a non-landmark weapon entry ships, its hash goes here and the
worked/redacted breakdown goes in the answer-key companion doc, same
shape as the other curtains.
